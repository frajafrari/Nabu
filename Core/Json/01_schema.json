{
    "title":"Ingreso a Nabu",
    "description":"Ingrese los datos solicitados",
    "type": "object",
    "properties": {
        "Campo1": {
            "type": "string",
            "required":true,
            "pattern": "^[a-zA-Z0-9_]+$"
        },
        "Campo2": {
            "type": "string",
            "format": "password",
            "required":true,
            "pattern": "^[a-zA-Z0-9_]+$"
        }
   }
}